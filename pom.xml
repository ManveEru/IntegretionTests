<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ru.manveru</groupId>
    <artifactId>IntegrationalTests</artifactId>
    <version>3.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>IntegrationalTests</name>
    <description>Integration tests for Education Server</description>

    <properties>
        <java.version>24</java.version>
        <maven.compiler.source>24</maven.compiler.source>
        <maven.compiler.target>24</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.plugin.version>3.11.0</maven.compiler.plugin.version>
        <maven.surefire.plugin.version>3.5.4</maven.surefire.plugin.version>
        <junit.jupiter.version>5.9.2</junit.jupiter.version>
        <rest-assured.version>5.3.0</rest-assured.version>
        <allure.version>2.24.0</allure.version>
        <aspectj.version>1.9.25</aspectj.version>
        <logback.version>1.4.14</logback.version>
    </properties>

    <dependencies>
        <!-- JUnit 5 Jupiter -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.24.2</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Allure JUnit 5 -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-junit5</artifactId>
            <version>${allure.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- REST Assured -->
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>${rest-assured.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>json-path</artifactId>
            <version>${rest-assured.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.14.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <version>3.0-rc5</version>
            <type>jar</type>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.3</version>
        </dependency>
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>5.1.0</version>
        </dependency>
        <!-- jOOQ -->
        <dependency>
            <groupId>org.jooq</groupId>
            <artifactId>jooq</artifactId>
            <version>3.19.7</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <!-- Maven плагин для генератора кода jOOQ -->
                <groupId>org.jooq</groupId>
                <artifactId>jooq-codegen-maven</artifactId>
                <version>3.19.7</version>

                <executions>
                    <execution>
                        <id>generate-jooq-sources</id>
                        <!-- Привязываем генерацию кода к фазе generate-sources в Maven,
                        чтобы при выполнении mvn compile код генерировался автоматически перед компиляцией -->
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>

                <configuration>
                    <!-- ***** Конфигурация подключения к БД (используем ваши данные) ***** -->
                    <jdbc>
                        <driver>org.postgresql.Driver</driver>
                        <url>jdbc:postgresql://localhost:5432/my_db</url>
                        <user>postgres</user>
                        <password>WenCoda</password>
                    </jdbc>

                    <!-- ***** Основная конфигурация генератора ***** -->
                    <generator>
                        <!-- Указываем, что генерируемый код будет на Java (это значение по умолчанию, но лучше указать явно) -->
                        <name>org.jooq.codegen.JavaGenerator</name>

                        <!-- Конфигурация базы данных: указываем jOOQ тип БД и что именно генерировать -->
                        <database>
                            <!-- Диалект базы данных PostgreSQL -->
                            <name>org.jooq.meta.postgres.PostgresDatabase</name>

                            <!-- Регулярное выражение, указывающее, какие объекты БД (таблицы, представления) включать.
                            Здесь мы включаем только таблицу "Employees". Обратите внимание на регистр - он должен соответствовать реальному имени в БД. -->
                            <includes>employees</includes>

                            <!-- Указываем, что исключать не нужно -->
                            <excludes></excludes>

                            <!-- Схема (Schema) базы данных, обычно "public" -->
                            <inputSchema>public</inputSchema>
                        </database>

                        <!-- ***** Опции генерации: управляем тем, какие типы кода создавать ***** -->
                        <generate>
                            <!-- Генерировать POJO классы (простые Java-объекты, содержащие только данные) -->
                            <pojos>true</pojos>
                            <!-- Генерировать Record классы, соответствующие записям в таблице. Это основа для работы с jOOQ -->
                            <records>true</records>
                            <!-- Генерировать DAO классы (объекты доступа к данным), которые предоставляют простые CRUD операции.
                            Это очень полезно для ваших "базовых операций" -->
                            <daos>true</daos>
                        </generate>

                        <!-- ***** Настройка целевой директории для сгенерированных файлов ***** -->
                        <target>
                            <!-- Пакет для сгенерированных классов, можете изменить под структуру вашего проекта -->
                            <packageName>ru.manveru.integrationaltests.DTO</packageName>
                            <!-- Директория для сгенерированного кода. Стандартная практика Maven - target/generated-sources/jooq -->
                            <directory>target/generated-sources/jooq</directory>
                        </target>
                    </generator>
                </configuration>

                <!-- Добавляем драйвер PostgreSQL как зависимость плагина, чтобы генератор мог подключиться к БД -->
                <dependencies>
                    <dependency>
                        <groupId>org.postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <!-- Используйте версию, которая уже есть в вашем проекте, или укажите совместимую -->
                        <version>42.7.3</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!-- Surefire Plugin for JUnit 5 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.plugin.version}</version>
                <configuration>
                    <argLine>
                        -javaagent:"${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar"
                    </argLine>
                    <systemPropertyVariables>
                        <property>
                            <name>allure.results.directory</name>
                            <value>${project.build.directory}/allure-results</value>
                        </property>
                    </systemPropertyVariables>
                    <useSystemClassLoader>false</useSystemClassLoader>
                    <useModulePath>false</useModulePath>
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                    </includes>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.aspectj</groupId>
                        <artifactId>aspectjweaver</artifactId>
                        <version>${aspectj.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            
            <!-- Allure Maven Plugin для генерации отчетов -->
            <plugin>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-maven</artifactId>
                <version>2.12.0</version>
                <configuration>
                    <reportVersion>${allure.version}</reportVersion>
                    <resultsDirectory>${project.build.directory}/allure-results</resultsDirectory>
                    <reportDirectory>${project.build.directory}/allure-report</reportDirectory>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>